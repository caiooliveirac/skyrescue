node_modules/
.env
.env.production
.env.*.local
*.log

# o template abaixo NÃO tem segredos e deve ser versionado
# (re-inclui apesar do `.env.*` do .gitignore raiz)
!.env.example

# wa-bridge: binário e sessão do WhatsApp ficam fora do repo
wa-bridge/wa-bridge
wa-bridge/*.db*
